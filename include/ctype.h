#ifndef CTYPES_H
#define CTYPES_H

const char* skip_spaces(const char* s);

bool isspace(char c);
bool isupper(char c);
bool islower(char c);
bool isalpha(char c);
bool isdigit(char c);

bool isdigits(const char* c);

#endif
