short term:
  futures
  networking
  cleanup kihm/khm/kvector/etc.
  make some new data structures (queue and set most notably)
  make userspace not use kernel malloc...


long term:
  preemption
  disk/stable storage
